<%
const tableId = table.id || 'table';
const tableClass = table.class || 'table table-hover align-middle mb-0 datatable-table w-100';
%>

<table id="<%= tableId %>" class="<%= tableClass %>"></table>
