<%- include('./labelInput', { 
    id: input.id, 
    label: input.label 
}) %>

<input id="<%= input.id %>" type="file" name="<%= input.name %>" 
    class="filepond" aria-describedby="<%= `${ input.id }Error` %>"
>

<%- include('./validationMessage', { 
    name: input.name, 
    errorClass: 'invalid-feedback' 
}) %>