<div class="mb-3 <%= col %>">
    <label for="<%= id %>" class="form-label"><%= label %></label>

    <select id="<%= id %>" style="width: 100%" class="form-select">
        <option></option>
    </select>
</div>
