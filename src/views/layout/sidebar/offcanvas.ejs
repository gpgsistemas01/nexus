<% const header = {
    button: {
        id: 'menuCloseBtn',
        dismissTarget: 'offcanvas'
    },
    class: 'offcanvas-header',
    title: {
        id: 'menuTitle',
        content: appName,
        class: 'offcanvas-title'
    }
}; %>

<div
    id="mobileMenu"
    tabindex="-1"
    class="app-mobile-menu offcanvas offcanvas-start"
    data-mdb-theme="dark"
    aria-labelledby="<%= header.title.id %>"
    aria-hidden="true"
>
    <%- include('../../shared/header', { header }) %>

    <div class="px-3 pb-3">
        <%- include('../ui/appBrand', { className: 'app-brand--menu' }) %>
    </div>

    <div class="offcanvas-body d-flex flex-column">
        <div class="app-mobile-menu__nav flex-grow-1 mb-5">
            <div class="mb-5">
                <%- include('../ui/navList', { 
                    listClass: 'navbar-nav flex-column app-mobile-menu__list',
                    showIcons: true
                }) %>
            </div>
        </div>

        <div class="app-mobile-menu__footer mt-auto">
            <%- include('../ui/logoutForm', {
                buttonClass: 'btn btn-outline-success app-logout-btn w-100'
            }) %>
        </div>
    </div>
</div>