<% const menuButton = {
    id: 'mobileMenuOffcanvasBtn',
    class: 'navbar-toggler p-2',
    attrs: {
        'data-mdb-ripple-init': '',
        'data-mdb-offcanvas-init': '',
        'data-mdb-target': '#mobileMenu',
        'aria-controls': 'mobileMenu',
        'aria-expanded': 'false'
    },
    content: '<i class="fas fa-bars me-2"></i>Menú'
}; %>

<div class="d-flex min-vh-100 bg-white">

    <%- include('./sidebar/offcanvas') %>

    <aside class="d-none d-md-block bg-dark text-white p-3" style="width:260px;">
        <nav class="navbar navbar-expand-lg bg-dark p-0">
            <div class="container-fluid justify-content-between">

                <div role="navigation">
                    <%- include('./ui/navList', { 
                        listClass: 'navbar-nav flex-column',
                    }) %>
                </div>
            </div>
        </nav>
    </aside>

    <div class="flex-fill d-flex flex-column">
        <header class="navbar bg-white shadow px-3">

            <div class="d-md-none">
                <%- include('../shared/button', { button: menuButton }) %>
            </div>

            <div class="d-flex align-items-center gap-3">
                <div class="dropdown">
                    <button
                        id="notificationsBellBtn"
                        type="button"
                        class="btn btn-link text-dark position-relative p-0"
                        data-mdb-dropdown-init
                        data-mdb-ripple-init
                        aria-expanded="false"
                    >
                        <i class="fas fa-bell fs-5"></i>
                        <span
                            id="notificationsUnreadCount"
                            class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger d-none"
                        >
                            0
                        </span>
                    </button>

                    <ul
                        id="notificationsMenu"
                        class="dropdown-menu dropdown-menu-end p-0"
                        aria-labelledby="notificationsBellBtn"
                        style="min-width: 320px;"
                    >
                        <li class="px-3 py-2 border-bottom d-flex justify-content-between align-items-center">
                            <strong>Notificaciones</strong>
                            <button id="markNotificationsReadBtn" class="btn btn-sm btn-outline-secondary">Marcar leídas</button>
                        </li>
                        <li id="notificationsList" class="list-group list-group-flush"></li>
                    </ul>
                </div>

                <span class="text-dark"><%= user.name %></span>

                <%- include('./ui/logoutForm', {
                    buttonClass: 'btn btn-sm btn-outline-secondary ms-2'
                }) %>
            </div>
        </header>

        <main class="container-fluid p-3 p-md-4">
            <%- body %>
        </main>
    </div>
</div>

<script>
    window.meta = <%- JSON.stringify(user || {}) %>;
</script>
