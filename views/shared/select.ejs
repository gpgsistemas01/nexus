<div>
    <select id="<%= input.id %>" name="<%= input.name %>" style="width: 100%" class="form-select <%= input.class %>">
        <option></option>
    </select>

    <%- include('./validationMessage', { 
        name: input.name, 
        errorClass: input.errorClass || 'invalid-feedback' 
    }) %>
</div>